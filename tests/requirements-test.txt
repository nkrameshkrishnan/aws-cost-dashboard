# =============================================================================
# Test project dependencies
# =============================================================================
# Install with:
#   pip install -r tests/requirements-test.txt -r backend/requirements.txt
#
# These are test-only packages on top of the backend's production requirements.
# =============================================================================

# Test runner and plugins
pytest==9.0.2
pytest-asyncio==1.3.0
pytest-cov==7.0.0

# HTTP test client (used by FastAPI TestClient)
httpx==0.28.1

# Fake Redis for unit tests (no real Redis needed)
fakeredis==2.28.1

# AWS mocking
moto[all]==5.1.21

# Type stubs (optional, for IDE support in tests)
types-requests
