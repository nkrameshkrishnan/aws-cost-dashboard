# FastAPI and server
fastapi==0.135.1
uvicorn[standard]==0.41.0
gunicorn==25.1.0           # 21.2.0 had CVE-2024-1135 (HTTP request smuggling)
python-multipart==0.0.22   # 0.0.6 had CVE-2024-53981, CVE-2024-53981, CVE-2026-24486

# AWS SDK
boto3==1.42.63
botocore==1.42.63

# Database
sqlalchemy==2.0.48
psycopg2-binary==2.9.11
alembic==1.18.4

# Redis cache
redis==7.3.0               # 5.x → 7.x: removed deprecated top-level aliases; see migration notes
hiredis==3.3.0

# Authentication & Security
# NOTE: python-jose 3.x has unpatched algorithm-confusion CVEs (PYSEC-2024-232,
# PYSEC-2024-233) that have no fix in the library.  A follow-up task (Phase 3)
# should migrate app/core/security.py to PyJWT (pip install PyJWT cryptography).
python-jose[cryptography]==3.5.0
passlib==1.7.4
bcrypt==5.0.0              # 4.x → 5.x: bcrypt.hashpw() now returns str, not bytes
python-dotenv==1.2.2
cryptography==46.0.5       # 42.0.2 had PYSEC-2024-225, GHSA-h4gh-qq45-vh27, CVE-2024-12797

# Data processing
pandas==2.3.3
numpy==2.2.6               # 1.x → 2.x: removed legacy aliases; run test suite after upgrade
scipy==1.15.3
scikit-learn==1.7.2        # 1.4.0 had PYSEC-2024-110 (TF-IDF data leakage)

# Export & Reporting
reportlab==4.4.10
xlsxwriter==3.2.9
openpyxl==3.1.5
matplotlib==3.10.8
Pillow==12.1.1             # 10.2.0 had CVE-2024-28219 (buffer overflow in _imagingcms)

# Microsoft Teams integration
pymsteams==0.2.2

# Utilities
pydantic==2.12.5
pydantic-settings==2.13.1
python-dateutil==2.9.0.post0
pytz==2026.1

# Background jobs
apscheduler==3.11.2

# HTTP requests
requests==2.32.5           # 2.31.0 had CVE-2024-35195, CVE-2024-47081
httpx==0.28.1

# Validation
email-validator==2.3.0

# CORS - handled via FastAPI middleware
# python-cors is not needed for FastAPI

# Jenkins AI Monitor
langgraph==1.0.10          # 0.2.16 had CVE-2026-28277 (RCE via msgpack checkpoints)
anthropic==0.84.0
python-jenkins==1.8.3
PyGithub==2.8.1
GitPython==3.1.46

# Development dependencies
pytest==9.0.2
pytest-asyncio==1.3.0     # 0.x → 1.x: asyncio_mode config option moved; update pytest.ini
pytest-cov==7.0.0
moto==5.1.21
black==26.3.0              # 24.1.1 had PYSEC-2024-48 (ReDoS)
flake8==7.0.0
mypy==1.19.1
